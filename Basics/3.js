// your cannot use 1value because var cannot make 1 in start but u can use it after wards.
var value1 = 10;
console.log(value1**0.5);
// you can use only _(underscore) or  $)(dollar) only.
//**cannot use space in var(variable).
